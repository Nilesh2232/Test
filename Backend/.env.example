# Application
APP_NAME=Support Ticketing CRM
DEBUG=true

# Leave DATABASE_URL unset to use project-root tickets.db (outside Backend/).
# Or set an absolute sqlite URL if you need to override:
# DATABASE_URL=sqlite:///D:/Learning/Datastraw/tickets.db
